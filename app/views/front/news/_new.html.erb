<div>
  <span><%=l(new.created_at)%></span>

  <% unless new.image.blank? %>
    <%= image_tag new.image, :alt => "", :class => 'thumbnail_news' %>
  <% end %>

  <h1><%=new.title%></h1>

  <span>
    <%=simple_format(new.intro)%>
  </span>
</div>
